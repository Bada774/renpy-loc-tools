# add_context.py auto-detects this game (no -g needed) when the run path
# contains any of these substrings. Add as many as you like - any one matching
# is enough.
PROJECT_MARKERS = ["Fetish Locator S&M Studio"]

# Which languages to process by default when --lang isn't given. Leave unset
# (or empty) to process every language folder found under tl/.
LANGUAGES = ["russian"]